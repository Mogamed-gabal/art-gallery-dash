import { Component, EventEmitter, Output, inject } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
@Component({ selector: 'app-category-modal', standalone: true, imports: [ReactiveFormsModule], styleUrl: './category-modal.scss', template: `<form [formGroup]="form" (ngSubmit)="save.emit(form.getRawValue())"><input placeholder="اسم التصنيف بالعربية" formControlName="nameAr"><input placeholder="اسم التصنيف بالإنجليزية" formControlName="nameEn"><input placeholder="slug مثل abstract" formControlName="slug"><button class="primary-button" [disabled]="form.invalid">حفظ التصنيف</button></form>` })
export class CategoryModalComponent { private readonly fb = inject(FormBuilder); @Output() save = new EventEmitter<{ nameAr: string; nameEn: string; slug: string }>(); readonly form = this.fb.nonNullable.group({ nameAr: ['', Validators.required], nameEn: ['', Validators.required], slug: ['', [Validators.required, Validators.pattern(/^[a-z0-9]+(?:-[a-z0-9]+)*$/)]] }); }
