import { LoginPageComponent } from './login-page.component';
describe('LoginPageComponent', () => { it('is declared as a standalone login feature', () => { expect(LoginPageComponent).toBeDefined(); }); });
