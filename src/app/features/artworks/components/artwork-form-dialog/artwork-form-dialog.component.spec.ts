import { ArtworkFormDialogComponent } from './artwork-form-dialog.component';
describe('ArtworkFormDialogComponent', () => { it('is declared as a standalone form component', () => { expect(ArtworkFormDialogComponent).toBeDefined(); }); });
